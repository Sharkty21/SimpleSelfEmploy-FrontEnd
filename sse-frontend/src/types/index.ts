export type IJob = {
    id: string;
    name: string;
    description: string;
    startDate: Date;
    customerName: string;
}

export type IPayment = {
    id: "string",
    amount: "int",
    date: Date
}